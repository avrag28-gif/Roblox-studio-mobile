#pragma once
#include "../core/base_part.h"
#include "../math/aabb.h"
namespace rsm {struct RaycastHit{BasePart*part=nullptr;float distance=0;Vector3 position{};};class PhysicsWorld{Vector3 gravity_;public:explicit PhysicsWorld(Vector3 g={0,-196.2f,0}):gravity_(g){}void Step(float dt,const Instance&root){for(auto*x:root.GetDescendants())if(auto*p=dynamic_cast<BasePart*>(x))if(!p->Anchored()){auto v=p->Position()+gravity_*dt;if(v.y<0)v.y=0;p->SetPosition(v);}}RaycastHit Raycast(const Instance&root,const Ray&r)const{RaycastHit best;best.distance=1e30f;for(auto*x:root.GetDescendants())if(auto*p=dynamic_cast<BasePart*>(x))if(p->CanQuery()){auto h=p->Size()*0.5f;AABB b{p->Position()-h,p->Position()+h};float t;if(b.IntersectRay(r,t)&&t<best.distance)best={p,t,r.At(t)};}if(!best.part)best.distance=0;return best;}Vector3 Gravity()const{return gravity_;}};}