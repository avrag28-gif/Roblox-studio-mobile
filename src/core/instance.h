#pragma once
#include "signal.h"
#include <memory>
#include <string>
#include <unordered_map>
#include <variant>
#include <vector>
namespace rsm{using AttributeValue=std::variant<std::monostate,bool,double,std::string>;class Instance{public:explicit Instance(std::string="Instance");virtual~Instance();const std::string&Name()const noexcept{return name_;}void SetName(std::string);const std::string&ClassName()const noexcept{return className_;}Instance*Parent()const noexcept{return parent_;}bool Archivable()const noexcept{return archivable_;}void SetArchivable(bool)noexcept;void SetParent(Instance*);static Instance*SetParent(std::unique_ptr<Instance>,Instance*);Instance*FindFirstChild(const std::string&,bool=false)const;Instance*FindFirstChildOfClass(const std::string&)const;std::vector<Instance*>GetChildren()const;std::vector<Instance*>GetDescendants()const;void Destroy();virtual std::unique_ptr<Instance>Clone()const;void SetAttribute(std::string,AttributeValue);const AttributeValue*GetAttribute(const std::string&)const;Signal<Instance*>ChildAdded,ChildRemoved;Signal<const std::string&>AttributeChanged,PropertyChanged;Signal<>Destroying;Signal<const std::string&>&GetPropertyChangedSignal(const std::string&){return PropertyChanged;}protected:void AddChild(std::unique_ptr<Instance>);void CopyStateTo(Instance&)const;void CloneChildrenTo(Instance&)const;private:std::string name_="Instance",className_;Instance*parent_=nullptr;bool archivable_=true,destroyed_=false;std::vector<std::unique_ptr<Instance>>children_;std::unordered_map<std::string,AttributeValue>attributes_;friend class DataModel;};}